Download the following files into this directory before running `Leduc_prep_for_NIFty.py`.

1. Navigate to `https://drive.google.com/drive/folders/12-H2a1mfSHZUGf8O50Cr0pPZ4zIDjTac `
2. Download `t6.csv`, `t7.csv`, and `meta.csv`
3. Move `t6.csv`, `t7.csv`, and `meta.csv` into this directory

Run `Leduc_prep_for_NIFty.py` to prep the data.
